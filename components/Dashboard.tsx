import { sidebarData } from "@/data/sidebar";
import DashboardWrapper from "./DashboardWrapper";

const Dashboard = () => {
  return <DashboardWrapper sidebarData={sidebarData} />;
};

export default Dashboard;
